<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.apppilates">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ClienteActivity"></activity>
        <activity android:name=".ProfissionalActivity" />
        <activity
            android:name=".Proficional"
            android:label="@string/title_activity_proficional"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".profissional" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SenhaActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>